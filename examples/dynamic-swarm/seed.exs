# Minimal seed for the dynamic-swarm demo.
#
# This is intentionally empty — the demo grows the swarm at runtime via
# SwarmManager.add_agent, add_object, and scale_agent_group.

%{
  name: "dynamic-demo",
  agents: [],
  objects: [],
  topology: []
}
